# ClaudeCode_MyTools 專案規則

## 環境重掃（含每週一次的自動重掃）
`ClaudeCode_MyTools.html` 的 `TOOLS` 陣列裡，**`type: "待試・未安裝"` 的條目是刻意保留的待辦清單，不是環境掃描結果**。

- 這些條目本來就沒裝，環境裡掃不到是正常的——**絕對不要因為掃不到就當「已移除」刪掉**，除非使用者當次明確要求移除。
- 每個這類條目的 `note` 開頭也會有防護句（「刻意保留的待辦條目…」），但那是給人看的雙重保險，不是唯一防線；這條規則本身才是真正的判斷依據。
- 新增待辦條目時，`note` 開頭請照既有格式加上同一句防護句，維持一致。

目前待辦條目：book-to-skill（把書轉成 agent skill）、claude-seo（SEO 稽核 plugin）、Open Slide（投影片即程式碼框架）、phone-harness（讓 agent 操控真實 iPhone）、OfficeCLI（Office 文件自動化 CLI）。

## 這是公開網頁：只寫工具，不寫個人資訊
`ClaudeCode_MyTools.html` 會 push 到公開的 GitHub repo 並用 GitHub Pages 上線，**要當成陌生人會看到的東西來寫**。重掃時新增或改寫任何條目，都套用以下界線：

- **寫「這個工具能做什麼、怎麼叫它」；不寫「授權怎麼來的」。** 可以寫「TDX 可以查公車到站」「Firecrawl 可以爬站，免費額度有限」；不要寫憑證放哪、走哪種認證、API key 設好了沒、哪些指令在放行名單上、環境變數設成什麼值。
- **不寫身分與住處**：姓名、家庭成員、居住地區、macOS 使用者名稱（launchd label 若含使用者名稱就改寫成 plist 路徑）。
- **不寫精確的排程時間**：用「每晚」「每週一次」「每天固定時段」帶過，別列出幾點幾分——那等於公布作息與這台機器何時開著。
- **不寫營運與財務**：試用金金額與到期日、成本數字、品牌案子的進度狀態。
- **不寫個人知識庫的內部結構**：私人白板名稱、日記與健康相關的分區。專案資料夾名稱（`1_DiscordBot` 這類）可以留，它們是使用工具的必要資訊。

拿不準某句算不算越界，就寫保守的版本，並在該條目的 `note` 留一句提醒使用者確認。
